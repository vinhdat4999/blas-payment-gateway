apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Chart.Name}}-service-config
data:
  application.yaml: |
    server:
      port: 8083
    spring:
      profiles:
        active: default
      datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://blas.mysql.database.azure.com/blas
        username: blas
        password: 0546550566Qq@#
      data:
        mongodb:
          uri: mongodb+srv://vinhdat4999:hpDOksLujqSukEbt@blas.ofn7pbk.mongodb.net/blas
      jpa:
        hibernate:
          ddl-auto: none
        open-in-view: false
        show-sql: false
        properties:
          hibernate:
            dialect: org.hibernate.dialect.MySQL8Dialect
    management:
      endpoints:
        web:
          exposure:
            include: "*"
      endpoint:
        health:
          probes:
            enabled: true
    blas:
      aliasBlasPrivateKey: blasSecretKey
      certificate: BlasSecretKey.p12
      service:
        serviceName: blas-payment-gateway
      gate-interceptor:
        enableLogRequest: true
        enableLogLocalRequest: false
      blas-service-support:
        throughServiceSupport: true
        endpointCheckMaintenance: http://blas-service-support.dev.svc.cluster.local/maintenance/check
      blas-idp:
        isSendEmailAlert: false
        jwt:
          timeToExpiredInternalToken: 120 #2 minutes
      blas-payment-gateway:
        vnpay:
          version: 2.1.0
          tmnCode: RHC8EX66
          payUrl: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
          returnUrl: http://localhost:8083
          checkOrderUrl: https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
          vnpTxnRefLength: 8
          orderType: other
        lengthOfId: 10
      hazelcast:
        aksNamespace: dev
        hazelcastService: hazelcast
    logging:
      level:
{{- range $key, $value := index .Values "service-config" "logging" "level" }}
        {{$key}}: {{$value}}
{{- end }}

  hazelcast-client.yaml: |-
    hazelcast-client:
      network:
        kubernetes:
          enabled: {{ index .Values "service-config" "hazelcast-client" "network" "kubernetes" "enabled" }}
